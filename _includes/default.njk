<!DOCTYPE html>
<html lang="en">
  <head>
    <title>
      {% block title %}King's Quill{% endblock %}
    </title>
    <meta charset="UTF-8"/>
    {% if description %}
      <meta name="description" content="{{description}}"/>
    {% endif %}
    <meta http-equiv="x-ua-compatible" content="ie=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no, viewport-fit=cover"/>
    <link rel="stylesheet" href="css/style.css?v={% version %}"/>
    <link rel="stylesheet" href="css/fontawesome.css">
    {% block head %}{% endblock %}
  </head>
  <body class="{% block backgroundImage %}bg-home{% endblock %} bg-top bg-cover bg-fixed">
    <div class="container mx-auto bg-neutral-900/95 min-h-screen text-white text-xl">
      <header class="flex p-4 content-center justify-between">
        <a href="/" class="font-heading text-4xl">KQ</a>
        <div class="flex items-center">
          {% for page in collections.header %}
          <a class="p-2 m-2 font-serif hover:underline" href="{{page.url}}">
            {{page.data.title}}
          </a>
          {% endfor %}
          {% for social in site.socials %}
            {% if social.icon %}
              <a class="p-2 m-2" href="{{social.link}}">
                <i class="fa-brands {{social.icon}}"></i>
              </a>
            {% endif %}
          {% endfor %}
        </div>
      </header>
      {% block content %}
        {{ content | safe }}
      {% endblock %}
    </div>
  </body>
</html>